name: injectable_ordering
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  injectable: any
  get_it: any

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  injectable_generator: any
  build_runner: any
